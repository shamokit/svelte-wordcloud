---
"@shamokit/svelte-wordcloud": minor
---

WordCloudFlat: fix zoom slider initial position so zoom-out below 1× is accessible via the scrollbar (Ctrl+Wheel already worked). WordCloud3D: add zoom control (0.3×–3.0×) with a dedicated zoom scrollbar; Ctrl+Wheel now zooms in/out instead of navigating depth, allowing both zoom-out for clipped words and zoom-in on the last layer.
